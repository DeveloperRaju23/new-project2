import React from 'react'

const Heading = ({heading}) => {
  return (
    <div>
        <h1 className='text-[24px] text-primaryClr font-serifFont font-semibold'>{heading}</h1>
        
    </div>
  )
}

export default Heading